import React, { useState } from 'react';

  const editplayer = () => {
    
    };
  
  return (
    <div>
      <button onClick={editplayer}>Redigera spelare</button>
    </div>
  );


export default menu;
